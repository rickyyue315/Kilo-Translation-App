# Copy to .env (never commit real keys). Zeabur: paste these in the dashboard.
OPENROUTER_API_KEY=
# Optional — public URL of the deployed service (used for OpenRouter referer)
APP_URL=
# Optional — defaults to *
CORS_ORIGIN=*
PORT=3000
